configuration:
  language:
    name: Dashboard language
    description: >-
      Select the dashboard language. Use "de" for German or "en" for English.
  log_level:
    name: Log level
    description: >-
      Controls the level of log output from the add-on. "trace" is the most
      verbose, "fatal" is the least verbose.